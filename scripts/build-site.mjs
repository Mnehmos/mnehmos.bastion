#!/usr/bin/env node
// Stub: site builder. Reads bastion/biographies/* + themes/* + ledger/* -> docs/
// Real implementation per bastion-website-spec.md §2.1 / §6 and PUBLISHING.md step 3.
console.log('build-site stub');
process.exit(0);
