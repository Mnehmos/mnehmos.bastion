#!/usr/bin/env node
// Stub: hash-gated OpenAI TTS narrator. Per PUBLISHING.md step 3b.
// Reads each biography's theme.yaml `voice`, hashes narration text against
// docs/<...>/audio/.hashes.json, only re-synthesizes when changed.
console.log('narrate stub');
process.exit(0);
